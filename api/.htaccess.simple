# Simple .htaccess - nếu file trên không hoạt động, thử file này

# Enable PHP
AddHandler application/x-httpd-php .php

# Allow all methods
<Limit GET POST PUT DELETE OPTIONS>
    Order allow,deny
    Allow from all
</Limit>

# Prevent directory listing
Options -Indexes
